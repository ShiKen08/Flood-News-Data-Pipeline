# Flood Pipeline — Python dependencies
# Install with: pip install -r requirements.txt

# --- Core data ---
pandas>=2.0.0
pyarrow>=14.0.0          # Parquet read/write
numpy>=1.26.0            # Numeric arrays (Stage 05, NLP bridge)

# --- HTTP / web ---
requests>=2.31.0
urllib3>=2.0.0

# --- HTML extraction ---
trafilatura>=1.8.0        # Primary article body extractor (Stage 05)
beautifulsoup4>=4.12.0    # Fallback / utility parsing
lxml>=5.0.0               # Parser backend for BS4 and trafilatura

# --- Encoding detection ---
chardet>=5.0.0            # Stage 05 encoding detection

# --- Language detection ---
langid>=1.1.6             # Stage 06 language detection (replaces langdetect)

# --- Progress / CLI ---
tqdm>=4.66.0              # Progress bars

# --- Testing ---
pytest>=8.0.0             # Test runner
pytest-mock>=3.12.0       # Mocking helpers

# --- Async HTTP (Stage 04) ---
aiohttp>=3.9.0            # Async HTTP client